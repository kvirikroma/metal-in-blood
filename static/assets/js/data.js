let data = [
    {
        "id": 1,
        "login": "admin",
        "password": "admin"
    },
    {
        "id": 2,
        "login": "dev",
        "password": "dev"
    }
];


    // const xhr = new XMLHttpRequest();
    // xhr.open('GET', 'assets/js/users_db.json');
    // xhr.send();
    // xhr.addEventListener('readystatechange', () => {
    // if (xhr.status === 200 && xhr.readyState == 4) {
    //     data = xhr.response
    // } else return;
    // });

// users ? null : localStorage.setItem('users', JSON.stringify(data));
